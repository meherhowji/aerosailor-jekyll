{
    "scripts": {
        "deploy": "jekyll build && ./deploy.sh && git checkout production && git push live production && git checkout master"
    },
    "dependencies": {
        "aos": "^2.2.0",
        "bootstrap": "4.0.0-beta.3",
        "jquery": "3.0.0",
        "popper.js": "^1.12.9"
    },
    "devDependencies": {
        "aos": "^2.2.0",
        "bootstrap": "4.0.0-beta.3",
        "jquery": "3.0.0",
        "popper.js": "^1.12.9"
    }
}
