{
    "scripts": {
        "deploy":"jekyll build && ./deploy.sh && git checkout gh-pages && git push live gh-pages"
    },
    "dependencies": {
        "bootstrap": "4.0.0-beta.3"
    },
    "devDependencies": {
        "jquery": "3.0.0",
        "popper.js": "^1.12.9"
    }
}
